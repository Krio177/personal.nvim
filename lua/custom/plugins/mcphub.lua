return {
  -- 'ravitemer/mcphub.nvim',
  -- dependencies = {
  --   'nvim-lua/plenary.nvim',
  -- },
  -- build = 'npm install -g mcp-hub@latest', -- Globális telepítés
  -- config = function()
  --   require('mcphub').setup {
  --     -- Itt testreszabhatod a beállításokat, pl. port, log, stb.
  --     -- port = 3000,
  --     -- config = vim.fn.expand("~/.config/nvim/mcpservers.json"),
  --   }
  -- end,
}
